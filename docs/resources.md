# Useful Links / Resources

Resources are managed from `/admin/resources` and displayed publicly at `/app/resources`.

Rules:
- Public pages show only resources with `status: active`.
- URLs must be `http://` or `https://`.
- Archived and inactive resources stay in admin for history but are hidden from students.
- Featured resources are highlighted first in the public resource board.

Future Supabase table: `resources`.
